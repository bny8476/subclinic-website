spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
app:
  datasource:
    growth:
      url: jdbc:h2:mem:growthtestdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;NON_KEYWORDS=VALUE
      driver-class-name: org.h2.Driver
      username: sa
      password: ""

